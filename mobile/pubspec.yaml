name: plop
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  #Default - HTTP client
  http: ^1.2.2
  #Default - Router
  go_router: ^14.0.0
  #Default - I18n
  easy_localization: ^3.0.7
  #Maps
  geolocator: ^13.0.2

  #Default - Locale store
  shared_preferences: ^2.4.0
  #Default - Get Device Id
  device_info_plus: ^11.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/translations/
